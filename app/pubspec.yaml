name: marketplace_app
description: Multi-tenant e-learning marketplace.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  go_router: ^10.0.0
  supabase_flutter: ^2.0.0
  url_launcher: ^6.1.10
  common:
    path: ../packages/common

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
