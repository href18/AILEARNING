name: common
description: Shared models and API helpers for the AI learning marketplace.
version: 0.0.1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  meta: ^1.9.1
  supabase_flutter: ^2.0.0
